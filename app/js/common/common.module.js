
(function(){
    'use strict';

    var module = {
        name: 'multimage.common',
        dependencies: ['angularFileUpload']
    };

    angular
        .module(module.name, module.dependencies)
})();